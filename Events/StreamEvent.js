const  {EventEmitter} = require("events");

let streamProcessed = new EventEmitter();

const {
    Stream,
    RawStream,
    Content
} = require('../Models');

StreamEvent.on("completed",(data)=>{
    
});